# Контекст проекта

Telegram-бот для генерации Release Notes из Яндекс Трекера с публикацией
на сайт-дайджест и в TG-канал. Внутренний инструмент Skillaz (HR-tech).
Продукт состоит из двух частей: **Подбор** (найм) и **L&D** (обучение).

Общение и весь контент — на русском.

## Структура

- `bot.py` — диалог в Telegram (ConversationHandler), состояния:
  SELECT_RELEASES → SELECT_TASKS → ASK_MEDIA → WAIT_MEDIA → CONFIRM_NOTES → WAIT_FILE
- `tracker.py` — Яндекс Трекер API v3
- `deepseek.py` — генерация текста карточек
- `publisher.py` — сборка HTML, запись `releases_data.json`, пост в канал
- `webhook_server.py` — FastAPI на порту 8001, автопубликация по вебхуку
- `digest_template.html` — шаблон сайта, плейсхолдеры
  `<!-- CARDS_PLACEHOLDER -->` и `<!-- SIDEBAR_PLACEHOLDER -->`

## Сервер

Пути: код в `/opt/ld_bot/`, сайт в `/var/www/digest/`,
venv в `/opt/ld_bot/venv/`, токены в `/opt/ld_bot/config.env` (не в git).

Сервисы: `ld_bot`, `ld_webhook`. После правок кода —
`sudo systemctl restart ld_bot`.

Деплой: `scp` файла на сервер в `/tmp/`, затем `sudo cp` в `/opt/ld_bot/`.

## Яндекс Трекер

- Организация Яндекс 360 → заголовок `X-Org-ID` (не `X-Cloud-Org-Id`), id `3814554`
- Очередь `DEV`, релизы типа `release`, фичи типа `Story`
- `module` — кастомное поле, читается как `issue['module']`
- `project` — `{"primary": {"display": "..."}, "secondary": [...]}`
- `Product Development` в проекте → продуктовая фича, иначе проектная
- Исключаются: тег `Tech🔧`, типы `Ошибка`, `Ошибка с прода`, `Релиз`
- Клиенты по проекту: `UniRus hcm`→ЮниРусь, `Nestle hcm`→Nestle,
  `hh.ru hcm`→HeadHunter, `Skillaz hcm`→Skillaz

## Дизайн сайта

Шрифт Geologica. Фон `#f4f5f7`, карточки белые со скруглением 10px.

Карточка: шапка с названием (16px, без стрелки раскрытия) → блок
бизнес-ценности → описание → футер с тегами.
Теги в футере: модуль (серый пилл), Продукт/Проект (цветной пилл:
продукт `#e8f0fe`/`#1a5fa8`, проект `#fce8d8`/`#9a4500`), клиент,
ссылка «Открыть в ЯТ» справа.

Сайдбар светлый: Продукт (Все/L&D/Подбор, по умолчанию L&D) → Модули →
Период → Клиенты с поиском. Модули фильтруются по выбранному продукту
через `data-prod`. Активная кнопка переключателя — чёрная с белым текстом.

Фильтры сверху: Тип фичи (Все/Продуктовые/Проектные) и Вид
(Развёрнутый/Свёрнутый).

Заголовок страницы меняется: «Обновления L&D» / «Обновления Подбор» /
«Обновления». Параметр `?product=ld` в URL сразу выставляет нужный таб —
бот отдаёт такую ссылку при публикации.

## История и грабли

- Год в сайдбаре когда-то выводился как «202026» — двойной префикс `20`
  в `format_date_ru`. Проверять, что дата уже четырёхзначная.
- Даты в сайдбаре дублировались при нескольких публикациях за день —
  лечится `set()` перед сортировкой.
- Каждая карточка несёт `release_date` своей задачи, а не дату публикации:
  задачи из разных релизов должны попадать в разные даты.
- Медиа сохраняется в `/var/www/digest/media/`, расширение берётся из
  mime-type, иначе файлы падают как `.bin` и не отображаются.
- Токен Трекера живёт около года. Симптом протухания — 401 `not-found`
  или `bad oauth status: DISABLED`.
